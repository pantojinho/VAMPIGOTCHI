# VampGotchi Default Configuration
# This file is a template. User-specific settings go in config.yml (which is gitignored)

# Display Settings
display_mode: "black"  # "white" or "black" background
display_full_refresh_interval: 30  # Number of partial updates before full refresh

# Network Configuration
network_mode: "CLIENT"  # "AP" or "CLIENT"
ap_ssid: "VampGotchi-AP"
ap_pass: "vampgotchi123"
ap_ip: "192.168.4.1"

# BLEeding Integration
bleeding_path: "/root/BLEeding"
attack_timeout: 10  # seconds
scan_interval: 60  # seconds between automatic scans (0 = disabled)

# Debug Settings
debug_mode: false  # Enable verbose logging

